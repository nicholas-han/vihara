"""Econometric models — GARCH / EGARCH / GJR, ARIMA/VAR, cointegration, Kalman.

To fill (Q4). GARCH-family is the next model after the HAR baseline; it needs the
``arch`` package (declare as an optional extra). Each model implements the
``forecaster.core.Forecaster`` protocol.
"""
