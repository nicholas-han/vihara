"""Classical ML — trees, gradient boosting, linear, SVM (sklearn backend).

To fill (Q4). Models consume the shared HAR feature matrix and implement the
``forecaster.core.Forecaster`` protocol. Declare ``scikit-learn`` as an extra.
"""
