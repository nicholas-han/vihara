"""Deep learning — MLP, RNN/LSTM, TCN, Transformer (torch backend).

To fill (Q4). ``torch`` is a heavy optional extra (``forecaster[dl]``); nothing
here is imported unless explicitly used, so econometrics/ml users never pull it.
"""
