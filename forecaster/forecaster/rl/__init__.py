"""Reinforcement learning — environments + agents.

To fill (Q4), and the most likely candidate to spin out into its own module once
it grows a real training stack (ADR-6). Optional extra ``forecaster[rl]``.
"""
