[pytest]
# Research-stack packages are importable without install (each is its own root).
pythonpath = forecaster portfolio_manager strategies ledger instrument_manager plumber customized_orders
testpaths =
    forecaster/tests
    portfolio_manager/tests
    strategies/iv_rv_arb/tests
    ledger/tests
    instrument_manager/tests
    plumber/tests
    customized_orders/tests
addopts = -q
